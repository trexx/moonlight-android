---
name: Build

on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-java@v5
        with:
          java-version: 25
          distribution: 'corretto'

      - uses: android-actions/setup-android@v4
        with:
            packages: ''

      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build artifact
        run: ./gradlew build connectedCheck

      - name: Debug Sign
        run: jarsigner -verbose -keystore ~/.android/debug.keystore -storepass android -keypass android -signedjar app/build/outputs/apk/release/app-release-debugsigned.apk app/build/outputs/apk/release/app-release-unsigned.apk androiddebugkey

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: moonlight-android
          path: app/build/outputs/apk/release/app-release-unsigned.apk
          if-no-files-found: error
